theme = nord
font-size = 16
font-family = Iosevka Nerd Font Mono
font-feature = liga
shell-integration-features = no-cursor
cursor-style = block
window-padding-x = 16
window-padding-y = 16
# window-decoration = auto
macos-titlebar-style = hidden
link-previews = true


# unbind new tab since it has no decoration
# keybind = super+t=unbind

# Explicitly map the control keys to send the raw ASCII control character.
# This prevents them from being misinterpreted by macOS/terminal defaults.

# C-h (Left Navigation) -> ASCII 8 (Backspace/BS). 
# This is the most critical key to fix.
# keybind = ctrl+h=text:\x08

# C-j (Down Navigation) -> ASCII 10 (Line Feed/LF)
# keybind = ctrl+j=text:\x0A

# C-k (Up Navigation) -> ASCII 11 (Vertical Tab/VT)
# keybind = ctrl+k=text:\x0B

# C-l (Right Navigation) -> ASCII 12 (Form Feed/FF)
# keybind = ctrl+l=text:\x0C
